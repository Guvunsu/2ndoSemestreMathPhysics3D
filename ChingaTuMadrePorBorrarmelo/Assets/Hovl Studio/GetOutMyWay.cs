using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.Experimental.AI;

public class GetOutMyWay : MonoBehaviour
{
   // public float speed, dt, heightImpulse, forceImpulse;
   // private Rigidbody rb;

   //// bool isGround;
   // void Start()
   // {
   //     rb = GetComponent<Rigidbody>();
   // }

   // // Update is called once per frame
   // void Update()
   // {

   //     if (Input.GetKeyDown(KeyCode.Space))
   //     {
   //         rb.AddForce(transform.up * heightImpulse, ForceMode.Impulse);
   //     }

   // }
   // private void OnCollisionEnter(Collision collision)
   // {
   //     if (collision.collider.CompareTag("Floor"))
   //     {
   //         //GetComponent<Rigidbody>().AddForce(heightImpulse * transform.up, ForceMode.Impulse);
   //         isGround = true;

   //     }
   //     else isGround = false;
   // }

   // private void OnTriggerEnter(Collider other)
   // {
   //     if (other.CompareTag("Player"))
   //     {
   //         Vector3 force = heightImpulse * transform.up;
   //         rb.AddForce(force, ForceMode.Impulse);

   //     }

   // }
}
